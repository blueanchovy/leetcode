from collections import deque
class Solution:
    def duplicateZeros(self, arr: List[int]) -> None:
        q=deque()                                        
        for i in arr:
            q.append(i)
        cur=0
        while cur<len(arr):
            x=q.popleft()
            arr[cur]=x
            if x==0 & cur!=len(arr)-1:
                arr[cur+1]=x
                cur=cur+1
            cur=cur+1
            
